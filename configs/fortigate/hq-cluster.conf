#config-version=FGVMK6-6.2.3-FW-build1066-191219:opmode=0:vdom=0:user=admin
#
# Sanitized public reference export.
# Sensitive credentials, keys, certificates, HA passwords and IPsec PSKs removed.
# Original configuration retained privately by the project owner.

config system ha
    set group-name "HQ-Cluster"
    set group-id 1
    set priority 200
    set hbdev "port4" 50 "port5" 50
    set monitor "port1" "port2"
end

# Remaining configuration from the original export is intentionally represented
# in the public repository after sensitive values were removed.
